# Health Module

## Purpose

Simple health check endpoint for Railway deployment monitoring.

## Files

- `health.module.ts` — Module registration
- `health.controller.ts` — `GET /api/health` (public, returns 200)
